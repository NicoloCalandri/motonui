/**
 * Public layout — minimal shell for the public blog pages.
 * No authentication required.
 */
export default function PublicLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
